const COUNTRY_CODE = 'GE';
const WRONG_COUNTRY_CODE = 'GEEE';
const YEAR = 2022;
const WRONG_YEAR = 20222;

describe('Get list of public holidays', () => {
  test('Should call API with proper arguments and return list of public holidays', () => {});
  test('Should return 404 if wrong country is passed', () => {});
  test('Should return 400 if wrong year is passed', () => {});
});

describe('Check if today is public holiday', () => {
  test('', () => {});
});

describe('Get next public holidays', () => {
  test('Should call API with proper arguments and return list of next public holidays', () => {});
  test('Should return Error 500 if wrong country is passed', () => {});
});
